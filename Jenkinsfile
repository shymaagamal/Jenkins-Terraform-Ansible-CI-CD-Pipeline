pipeline {
    agent { label 'slave' }  

    stages {
        stage('Test Webhook') {
            steps {
                echo '✅ Hello, World! Webhook is working!'
            }
        }
    }
}
